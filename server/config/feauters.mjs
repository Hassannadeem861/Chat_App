const emitEvent = async (req, event, data, users) => {
  console.log("emitEvent :", event);
};

const deleteFromCloudinary = async (public_ids) => {};
console.log("deleteFromCloudinary: ", deleteFromCloudinary);

export { emitEvent, deleteFromCloudinary };
